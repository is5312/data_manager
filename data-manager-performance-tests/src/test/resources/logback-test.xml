<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>build/gatling-results/performance-test.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Gatling loggers -->
    <logger name="io.gatling" level="WARN"/>
    <logger name="io.gatling.http" level="WARN"/>
    
    <!-- Performance test loggers -->
    <logger name="com.datamanager.performance" level="INFO"/>
    
    <!-- gRPC loggers -->
    <logger name="io.grpc" level="WARN"/>
    <logger name="net.devh.boot.grpc" level="WARN"/>

    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
